# Copy this file to `.streamlit/secrets.toml` for local development.
# The real file is excluded by .gitignore. Never commit an actual API key.

[llm]
api_key = "replace-with-a-server-side-key"
model = "gpt-4.1-mini"
