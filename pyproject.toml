[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "portfolio-analysis"
version = "0.1.0"
description = "Portfolio CSV analysis Streamlit app"
requires-python = ">=3.11,<3.13"
dependencies = []

[tool.setuptools.packages.find]
include = ["app*"]
